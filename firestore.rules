rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All access via backend service account only — deny all direct client access
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
